Chimes AI platform layer 1 installed as release {{ .Release.Name }}.

Gateway base URL for products:

  https://{{ .Values.platform.gatewayHost | default "<set platform.gatewayHost>" }}

Smoke test (from inside the cluster):

  kubectl -n {{ .Release.Namespace }} run curl --rm -it --image curlimages/curl --restart=Never -- \
    curl -s http://{{ .Release.Name }}-litellm:4000/health/readiness
